<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".ui.features.digests.DigestsActivity" >

    <group
        android:id="@+id/digests_action_group_categories"
        android:orderInCategory="1000" >

        <item
            android:id="@+id/digests_action_categories"
            android:title="@string/digests_action_categories_title"
            android:icon="@drawable/ic_book"
            app:showAsAction="ifRoom" >

            <!-- Don't remove this SubMenu stub. Items could be added at runtime -->
            <menu />

        </item>

    </group>

    <group
        android:id="@+id/digests_action_group_about"
        android:orderInCategory="2000" >

        <item
            android:id="@+id/digests_action_about"
            android:title="@string/digests_action_about_title"
            android:icon="@drawable/ic_about"
            app:showAsAction="ifRoom"
            />

    </group>

</menu>